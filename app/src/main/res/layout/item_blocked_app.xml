<?xml version="1.0" encoding="utf-8"?>
<!--
  One app row, shaped like Focus Mode's: icon, name, checkbox on the right.
  Design §11 asks for icons and they were missing — a package name is not how
  anyone recognises an app.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/appIcon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="@null" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/appLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/font_body"
            android:textColor="@color/pxx_white_90"
            android:textSize="18sp" />

        <!-- Warn amber: a reason a row cannot be picked, or a caution about
             picking it, is status rather than decoration (§3.5). -->
        <TextView
            android:id="@+id/appReason"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:fontFamily="@font/font_body"
            android:lineSpacingMultiplier="1.25"
            android:textColor="@color/pxx_warn"
            android:textSize="12sp"
            android:visibility="gone" />

    </LinearLayout>

    <CheckBox
        android:id="@+id/appCheck"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:clickable="false"
        android:focusable="false" />

</LinearLayout>
