# Nope-Mode-todo4

Operator mill contract materialized from dispatch. Do not invent a different exam.

## Goal

Continue the millable product todo.md until the app is complete. Do not collapse into T1 src/package.json. Do not reuse the parent mill contract. Next boxes: Schedule window: apps suspend, QS tile reflects it, Take a break; Reboot mid-window: still suspended after boot.; DST / overnight: next window still fires.; Quiet Ringer: DND on; starred contacts still ring (dialer).; Relinquish on a **spare** or after a written backup — do not; Exact-alarm + DND + notification-listener grants survive an OS; Resync tokens from `piercingxx-branding` if Nope-Mode colors are.

### T1 — Continue the millable product todo.md until the app is complete. Do not colla...

- files: app/src/main/java/com/piercingxx/nopemode/data/Schedule.kt
- verify: ./gradlew :app:testDebugUnitTest --offline

## Final gate

- verify: ./gradlew testDebugUnitTest --offline
