# Nope-Mode-todo8

Operator mill contract materialized from dispatch. Do not invent a different exam.

## Goal

BK-1 — Ship `SuiteBackupProvider` at `${applicationId}.suite.backup` guarded by `com.piercingxx.suite.permission.BACKUP` plus the in-code signature check. Snapshot contains sche.... Locked STACK=android. Land the named leftover files — a leftover [x] without those files is still open. Do not tick leftover from T1–T7 exam placeholders (first-frame stitch, CameraX JPEG-only bind). Do not collapse into T1 src/package.json. Do not reuse the parent mill contract. Do not mill cleaned/audiobookshelf-app-cleanroom.

### T1 — Implement the scoped goal

- files: app/src/main/java/com/piercingxx/nopemode/service/TileLoader.kt
- verify: ./gradlew :app:testDebugUnitTest --offline

## Final gate

- verify: ./gradlew testDebugUnitTest --offline
