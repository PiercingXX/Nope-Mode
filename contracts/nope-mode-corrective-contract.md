# nope-mode-corrective-contract

Operator mill contract materialized from dispatch. Do not invent a different exam.

## Goal

Write the verify-sectioned contract at contracts/Nope-Mode.md (base HEAD 04edddc, package com.piercingxx.nopemode, code-side only — device-QA device-owner/relinquish are NOT in scope; theme permission declare already correct uses-only, do not touch). FIRST measure the tree and open the contract with a 'State of the tree (measured this session)' section. Gate measured this session: ./gradlew --no-daemon testDebugUnitTest --offline --rerun-tasks → BUILD SUCCESSFUL in 1m 5s, 24 test classes / 235 tests / 0 failures / 0 errors / 0 skipped (Kotlin daemon failed to connect on a read-only marker path but Gradle fell back to in-process compile). Every deliverable is a live gap — none already done. All five tasks below.

Declare tasks as '### T1 — …' sections. Each task MUST carry a '- files:' bullet naming the paths it changes (the enqueue lint refuses a task without one) and a '- verify:' bullet that runs behavior (a bare gradle unit-test command, quote-free, no backticks, no prose after the line — the whole line is handed to /bin/sh exactly as written). NO TWO TASKS MAY SHARE A VERIFY — each task gets its own new test class so its verify is unique. IF A TASK PROMISES A NAMED SYMBOL the verify must detect that symbol — the test calls it by name. End the contract with a '## Final gate' section naming ONE command (./gradlew --no-daemon testDebugUnitTest --offline) as a '- verify:' bullet; no individual task may claim that command. Include a Deferred section for anything the box cannot prove (on-device behavior).

T1 — Honor the launcher's Custom theme via the BACKGROUND extra. ThemeSyncReceiver.onReceive (ThemeSyncReceiver.kt:67) does presetForLabel(name) ?: return, so 'Custom' is a no-op; EXTRA_BACKGROUND (ThemeSyncReceiver.kt:83) is documented but unread. Read xx.launcher.extra.BACKGROUND and persist/apply a custom colour when presetForLabel returns null. SettingsStore gains a custom-colour holder; BackgroundTheme/BrandActivity apply it. Update the existing ThemeSyncReceiverTest assertion that 'a Custom theme persists nothing'. New test ThemeSyncCustomTest drives onReceive with a real Intent carrying EXTRA_BACKGROUND and a Custom name and asserts the custom ARGB is persisted. verify: env JAVA_HOME=$HOME/.local/android-toolchain/jdk17 ANDROID_HOME=$HOME/.local/android-toolchain/sdk ./gradlew --no-daemon testDebugUnitTest --tests com.piercingxx.nopemode.ui.ThemeSyncCustomTest. files: ThemeSyncReceiver.kt, BackgroundTheme.kt, BrandActivity.kt, SettingsStore.kt, ThemeSyncCustomTest.kt, ThemeSyncReceiverTest.kt.

T2 — Live-repaint Home when the theme changes, not only on resume. BrandActivity.applyTheme() (BrandActivity.kt:32-35) runs only from onResume. Extract a pure named decision ThemeChange that, given previously-applied vs newly-read theme, reports whether it changed; BrandActivity registers a preference/flow observer calling ThemeChange.changed(...) and repaints via applyTheme() when true. New test ThemeChangeTest calls ThemeChange.changed(old, new) by name. verify: env JAVA_HOME=$HOME/.local/android-toolchain/jdk17 ANDROID_HOME=$HOME/.local/android-toolchain/sdk ./gradlew --no-daemon testDebugUnitTest --tests com.piercingxx.nopemode.ui.ThemeChangeTest. files: ThemeChange.kt, BrandActivity.kt, ThemeChangeTest.kt.

T3 — Protect com.piercingxx.xxclock from suspension. ProtectedPackages.discover (ProtectedPackages.kt:48-95) hard-blocks self, admins, launcher, IME, installer, dialer, permission controller — not the clock, so a suspended clock misses alarms. Add the clock to the protected set so the picker does not offer it (hard block with a missed-alarm reason). New test ClockProtectionTest drives the pure SuspendablePackages decision with a set containing the clock and asserts it is excluded with the clock reason. verify: env JAVA_HOME=$HOME/.local/android-toolchain/jdk17 ANDROID_HOME=$HOME/.local/android-toolchain/sdk ./gradlew --no-daemon testDebugUnitTest --tests com.piercingxx.nopemode.enforce.ClockProtectionTest. files: ProtectedPackages.kt, SuspendablePackages.kt, ClockProtectionTest.kt.

T4 — Move the QS tile's Room access off the main thread. NopeTileService.onClick (NopeTileService.kt:45,46,54) and refresh (:63,64) call runBlocking on the main thread; Room under the shade can ANR. Extract a named suspendable TileLoader whose suspend fun load(...) reads the app-state and schedule DAOs and returns a derived TileSnapshot; NopeTileService calls it from a coroutine scope off the main thread. Add testImplementation org.jetbrains.kotlinx:kotlinx-coroutines-test to app/build.gradle. New test TileLoaderTest drives TileLoader.load(...) via runTest and asserts the snapshot. verify: env JAVA_HOME=$HOME/.local/android-toolchain/jdk17 ANDROID_HOME=$HOME/.local/android-toolchain/sdk ./gradlew --no-daemon testDebugUnitTest --tests com.piercingxx.nopemode.service.TileLoaderTest. files: app/build.gradle, NopeTileService.kt, TileLoader.kt, TileLoaderTest.kt.

T5 — Surface an empty blocked-app picker honestly. ProtectedPackages.launchableApps (ProtectedPackages.kt:98-107) uses queryIntentActivities; QUERY_ALL_PACKAGES denied on GrapheneOS returns empty and the picker shows a blank list. Extract a pure named AppPickerEmptyState that, given the launchable-app count, returns whether the picker is empty and the explanatory message; BlockedAppsActivity renders it instead of a blank page. New test AppPickerEmptyStateTest drives it with an empty list and asserts the empty-state message. verify: env JAVA_HOME=$HOME/.local/android-toolchain/jdk17 ANDROID_HOME=$HOME/.local/android-toolchain/sdk ./gradlew --no-daemon testDebugUnitTest --tests com.piercingxx.nopemode.ui.AppPickerEmptyStateTest. files: AppPickerEmptyState.kt, BlockedAppsActivity.kt, AppPickerEmptyStateTest.kt.

Deferred (box cannot prove, stays operator on-device): device-QA device-owner/relinquish (out of scope); QS tile shade repaint + ANR-free interaction on-device (design §16); visible Home live-repaint on-device. Final gate: - verify: env JAVA_HOME=$HOME/.local/android-toolchain/jdk17 ANDROID_HOME=$HOME/.local/android-toolchain/sdk ./gradlew --no-daemon testDebugUnitTest --offline. Write the contract file only — do not enqueue a mill.

### T1 — Write the verify-sectioned contract at contracts/Nope-Mode.md (base HEAD 04edddc, package com.pie...

- files: app/src/main/java
- verify: ./gradlew :app:testDebugUnitTest --offline

## Final gate

- verify: ./gradlew testDebugUnitTest --offline
