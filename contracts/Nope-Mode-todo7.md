# Nope-Mode-todo7

Operator mill contract materialized from dispatch. Do not invent a different exam.

## Goal

SMOKE-1 — Declare a launcher activity: give one Activity in AndroidManifest.xml an intent filter with MAIN action and LAUNCHER category. Locked STACK=android. Land the named leftover files — a leftover [x] without those files is still open. Do not tick leftover from T1–T7 exam placeholders (first-frame stitch, CameraX JPEG-only bind). Do not collapse into T1 src/package.json. Do not reuse the parent mill contract. Do not mill cleaned/audiobookshelf-app-cleanroom.

### T1 — SMOKE-1 — Declare a launcher activity: give one Activity in AndroidManifest.xml an intent filter with MAIN action and LAUNCHER category

- files: app/src/main/AndroidManifest.xml
- verify: python3 /home/piercingxx/.skippy/app/scripts/android_smoke.py . 2>&1 | tail -1 | grep -q 'SMOKE PASS'

## Final gate

- verify: ./gradlew testDebugUnitTest --offline
